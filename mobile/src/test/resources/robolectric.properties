# Pin to API 36 (this project's compileSdk/targetSdk) so Robolectric doesn't fall back to its
# own default framework SDK, which can lag behind compileSdk and fail to resolve an android-all
# artifact for a brand-new API level.
sdk=36
